#include <stdio.h>
#include <stdlib.h>

/**
 * main - returns alphabets
 *
 * Return: Always (0) (Scuccess)*
 */

int main(void)

{

char i;

for (i ='a'; i <= 'z'; i++)
putchar(i);
putchar('\n');

return (0);

}	
